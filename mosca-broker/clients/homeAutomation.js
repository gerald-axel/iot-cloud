module.exports = {
	homeAutomation : (json, mongoInstance) => {
		switch (json.Accion) {

			/* POST  Encender/Apagar */
			/*
			case 0:
				return  { "Accion": json.action , "Peso": json.time };			
			*/
			default:
				break;
		}
	}
}